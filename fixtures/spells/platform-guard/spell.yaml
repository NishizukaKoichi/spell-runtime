id: fixtures/platform-guard
version: 1.0.0
name: Platform Guard
summary: platform mismatch ガード確認
inputs_schema: ./schema.json
risk: low
permissions: []
effects:
  - type: deploy
    target: local
    mutates: false
billing:
  enabled: false
  mode: none
  currency: USD
  max_amount: 0
runtime:
  execution: host
  platforms:
    - never/platform
steps:
  - uses: shell
    name: noop
    run: steps/noop.js
checks:
  - type: exit_code
    params: {}
